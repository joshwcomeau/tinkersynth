# Pin Node version (also use .nvmrc)
[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps --ignore-scripts"

[build]
  # Rebuild sharp so it installs the Linux native binary (main install uses --ignore-scripts)
  command = "npm rebuild sharp && npm run build"
  publish = "public"
