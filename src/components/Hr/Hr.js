import React from 'react';
import styled from 'styled-components';

const Hr = styled.div`
  width: 50%;
  margin: auto;
  height: 1px;
  background: rgba(128, 128, 128, 0.25);
`;

export default Hr;
