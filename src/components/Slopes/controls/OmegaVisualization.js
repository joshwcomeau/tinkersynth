// @flow
import React from 'react';

import { COLORS } from '../../../constants';

type Props = {
  size: number,
  value: number,
};

const OmegaVisualization = ({ size, value }: Props) => {
  return null;
};

export default OmegaVisualization;
